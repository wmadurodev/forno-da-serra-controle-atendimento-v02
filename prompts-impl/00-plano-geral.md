# Controle de Atendimento — Plano de Implementação

> **Escopo deste documento:** controlar a execução faseada da implementação do app "Controle de Atendimento", garantindo rastreabilidade entre as decisões documentadas em `docs/*.md` e o código gerado.
> Cada passo tem um prompt de implementação dedicado em `prompts-impl/passo-NN-<slug>.md`. Um novo prompt só é criado depois que o passo anterior estiver implementado e validado manualmente.

## 1. Visão Geral

| Aspecto | Definição |
|---|---|
| Nome de exibição do app | **Controle de Atendimento** |
| Plataforma | Flutter, **Android** exclusivamente, 100% offline, sem autenticação (`docs/controle-atendimento-non-functional.md`) |
| Persistência | SQLite local (`docs/controle-atendimento-non-functional.md` §4) |
| Código-fonte final | `code/frontend/controle-atendimento-app` |
| Gerenciamento de estado | **Provider** — decisão técnica desta iniciativa de implementação, ver **IT-1** |

## 2. Documentos de Referência

| Documento | Conteúdo relevante para a implementação |
|---|---|
| `docs/controle-atendimento-functional.md` | Regras de negócio, máquina de estados do Pedido, critérios de validação. |
| `docs/controle-atendimento-data-structure.md` | Entidades, atributos, nomes técnicos (`snake_case`), enumerações. |
| `docs/controle-atendimento-non-functional.md` | Plataforma, mapeamento SQLite, ambiente de desenvolvimento (asdf, Android SDK). |
| `docs/controle-atendimento-prototype.md` | Telas, navegação, campos de UI, cards do quadro Kanban. |

## 3. Decisões Técnicas de Implementação (IT-*)

Decisões que não vêm da documentação funcional/não funcional, tomadas para viabilizar a implementação. Servem de referência para todos os passos — não devem ser repetidas em cada prompt de passo.

| ID | Decisão |
|---|---|
| IT-1 | Gerenciamento de estado: **Provider**. |
| IT-2 | Acesso ao SQLite: pacote **sqflite** (+ `path`/`path_provider` para localizar o arquivo do banco e as imagens). |
| IT-3 | Navegação: **Navigator 1.0** (`MaterialPageRoute`, push/pop) — sem roteamento declarativo, dado o número reduzido de telas e a ausência de deep-linking (app 100% offline, single-user). |
| IT-4 | Estrutura de pastas **feature-first**: `lib/features/<feature>/{data,domain,presentation}`, com `lib/core` para itens compartilhados (banco de dados, tema, widgets comuns). |
| IT-5 | Nome técnico do pacote Flutter (`pubspec.yaml`): `controle_atendimento_app`. Android `applicationId`: `com.fornodaserra.controleatendimento` (ajustável, sem impacto no domínio funcional). |
| IT-6 | SDK do Flutter fixado via `asdf` em `code/frontend/controle-atendimento-app/.tool-versions` (já commitado). |

## 4. Passos do Plano

| Passo | Nome | Depende de | Status | Prompt |
|---|---|---|---|---|
| 1 | Aplicação base, banco de dados, Splash e Home | — | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-01-app-base-splash-home.md` |
| 2 | Cadastro do Fluxo de Atendimento (Tela 3) | Passo 1 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-02-cadastro-fluxo-atendimento.md` |
| 3 | Execução do Fluxo de Atendimento — quadro Kanban (Tela 4) | Passos 1, 2 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-03-quadro-atendimento.md` |
| 4 | Cadastro de Pedido (Tela 5), integrado ao Kanban do Passo 3 | Passo 3 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-04-cadastro-pedido.md` |
| 5 | Execução de Pedido (Tela 6), integrado ao Kanban do Passo 3 | Passos 3, 4 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-05-execucao-pedido.md` |
| 6 | Edição de Pedido em Execução (Tela 9), integrado ao Kanban do Passo 3 | Passos 3, 5 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-06-edicao-pedido-execucao.md` |
| 7 | Cancelamento de Pedido (Tela 7), integrado ao Kanban do Passo 3 | Passo 3 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-07-cancelamento-pedido.md` |
| 8 | Devolução de Entrega (Tela 8), integrado ao Kanban do Passo 3 | Passos 3, 5 | **Em especificação** | `prompts-impl/passo-08-devolucao-entrega.md` |
| 9 | Botão "Sair" no header da Tela 4 (Execução do Fluxo de Atendimento) | Passo 3 | **Em especificação** | `prompts-impl/passo-09-sair-quadro-atendimento.md` |
| 10 | Fechar Fluxo de Atendimento a partir da Home (Tela 2) | Passo 1 | **Em especificação** | `prompts-impl/passo-10-fechar-fluxo-atendimento.md` |
| 11 | Melhorias na Home (Tela 2): nova chave sequencial de ordenação, layout em cards, novo ícone de fechamento, cor de fundo dos fluxos fechados, exclusão total de fluxo + pedidos | Passos 1, 10 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-11-melhorias-home.md` |
| 12 | Data/hora de cada mudança de estado do Pedido (8 novos campos), exibição do horário no título do card e destaque de pedido cancelado | Passos 1–9 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-12-timestamps-pedido.md` |
| 13 | Melhorias na Execução do Fluxo de Atendimento (Tela 4): remoção do botão de voltar, fundo sólido por coluna do Kanban (tom único, escurecendo à direita), header mais estilizado, ordenação decrescente por data/hora, filtro de visualização (Todos/Cancelados/Não Cancelados), espaçamento e cantos arredondados entre colunas, títulos centralizados, cards com paleta própria e cancelamento mais acentuado | Passos 3, 9, 12 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-13-melhorias-quadro-atendimento.md` |
| 14 | Melhorias nos cards de Pedido (Tela 4): campos de `aguardando_atendimento` visíveis quando preenchidos, botões de ação viram ícones centralizados, nome do cliente sem label e em negrito, labels dos demais campos viram ícones, imagem clicável em tela cheia, reordenação de campos (Endereço/Entrega, Mesa/Cliente), Observação em negrito, coluna "Retirado no Balcão" movida para o final | Passos 3, 12, 13 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-14-melhorias-cards-pedido.md` |
| 15 | Rótulo em cinza abaixo de cada ícone de ação no rodapé dos cards de Pedido (Tela 4) | Passo 14 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-15-labels-footer-cards.md` |
| 16 | Correção de bug: botão "Concluído" ausente no teclado dos campos "Motivo" multilinha (Telas 7 e 8) | Passos 7, 8 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-16-fix-teclado-campo-motivo.md` |
| 17 | Chave primária sequencial (`id INTEGER PRIMARY KEY AUTOINCREMENT`) na tabela `pedido`, substituindo `identificador`; correção necessária em `PedidoRepository.salvar()` (deixa de usar `ConflictAlgorithm.replace`) para o `id` não mudar a cada edição | Passo 12 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-17-pk-sequencial-pedido.md` |
| 18 | Ao digitar o identificador na Tela 5, buscar (em todos os fluxos, correspondência exata) o Pedido mais recente com aquele identificador e usar seus dados de Cadastro (exceto Observação) como molde para um Pedido **novo**; `pedido.identificador` deixa de ter constraint de unicidade; correção necessária em todo `PedidoRepository` e nas 5 telas de Pedido para operar por `id` em vez de `identificador` | Passo 17 | **Concluído e validado** (2026-09-14) | `prompts-impl/passo-18-template-identificador-pedido.md` |

| 19 | Renomear o rótulo "Atendimento" para "Atender" no ícone de ação do card `aguardando_atendimento` (Tela 4) | Passo 15 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-19-label-atender.md` |
| 20 | Correção de bug: `ImagemPedidoStorage` passa a nomear o arquivo pelo `id` do Pedido em vez do `identificador` (que deixou de ser único no Passo 18, podendo causar colisão/sobrescrita de imagens entre Pedidos diferentes) | Passos 17, 18 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-20-imagem-por-id.md` |
| 21 | Ao excluir definitivamente um Fluxo de Atendimento, excluir também os arquivos de imagem dos Pedidos vinculados a ele (hoje ficam órfãos em `pedidos_imagens/`) | Passos 5, 11, 20 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-21-excluir-imagens-fluxo.md` |
| 22 | Tipo de Pagamento só é obrigatório para o Pedido alcançar `em_atendimento` quando o Tipo de Entrega é `delivery`; para `retirada_balcao` pode ficar em branco nesta fase (Telas 5 e 9) | Passos 4, 6 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-22-pagamento-condicional-retirada.md` |
| 23 | Na Tela 5, ao editar um Pedido já além de `aguardando_atendimento` (via "Editar" no card `em_atendimento`), o campo Endereço passa a ser obrigatório (bloqueia o Gravar) se o Tipo de Entrega for `delivery` | Passo 4, 22 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-23-endereco-obrigatorio-edicao.md` |
| 24 | No card `em_execucao`, rótulo "Enviado" → "Enviar"; diálogo de confirmação simples antes de "Enviar", "Retirado" (card `em_execucao`) e "Entregue" (card `enviado`) | Passo 15 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-24-confirmacao-acoes-card.md` |
| 25 | Novo footer na Tela 4 com um navegador de 7 ícones (um por fase do Pedido, mesma ordem das colunas do Kanban), fundo redondo na cor real da coluna correspondente e contagem de cards abaixo; tocar rola o Kanban até a coluna daquela fase, alinhada à esquerda; mesmo ícone também no cabeçalho de cada coluna | Passos 13, 14 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-25-navegador-fases-footer.md` |
| 26 | Colunas/ícones de `entregue`, `devolvido` e `retirado_no_balcao` passam a usar cinza sólido (progressivamente mais escuro entre si), em vez da cor laranja sólida das demais fases | Passos 13, 25 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-26-degrade-cinza-status-finais.md` |
| 27 | Implementa a Busca de Pedidos (**P-7**, `functional.md` §5.5) — 6 campos (Identificador, Nome, Endereço, Mesa, Observação, Restrição), um por vez, Mesa exata e demais parciais, resultados em tela separada, agrupados por status/ordenados como o Kanban, em blocos verticais com os mesmos botões de ação | Passos 3, 13, 14, 25, 26 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-27-busca-pedidos.md` |
| 28 | No navegador de fases do footer da Tela 4, destacar (anel em volta do ícone) a fase cuja coluna está visível no Kanban; acompanha tanto a rolagem manual quanto o toque nos ícones do footer; destaque só em modo retrato | Passo 25 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-28-highlight-navegador-fases.md` |
| 29 | Na Home, novo ícone de cifrão (antes do ícone de fechamento) em cada linha de fluxo, abrindo um popup com os Pedidos daquele fluxo que têm valor de pagamento lançado ("(Tipo de Pagamento) Nome" + Valor) e o total somado | Passos 1, 5 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-29-valores-pagamento-home.md` |
| 30 | Campo Valor Pagamento (Telas 6 e 9) ganha máscara de entrada (só dígitos e um separador decimal, no máximo 2 casas) e validação ao gravar (valor monetário válido) | Passos 5, 6 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-30-validacao-valor-pagamento.md` |
| 31 | Foto do Pedido deixa de ser obrigatória nas Telas 6 e 9 (reverte **DS-7**/`data-structure.md` §3) | Passos 5, 6 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-31-foto-opcional.md` |
| 32 | Campo Valor Pagamento (Telas 6 e 9) ganha limite de 8 caracteres (incluindo o ponto/vírgula decimal) | Passo 30 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-32-limite-valor-pagamento.md` |
| 33 | Na Home, novo ícone (moto) por fluxo abre popup de Prestação de Contas com o Motoqueiro: lista Pedidos `delivery` (nome + horário de envio, endereço) e totaliza o número de entregas | Passos 1, 29 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-33-prestacao-contas-motoqueiro.md` |
| 34 | O popup de Valores de Pagamento (Passo 29) deixa de listar Pedidos cancelados | Passo 29 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-34-valores-pagamento-exclui-cancelados.md` |
| 35 | No footer/cabeçalho de coluna do Kanban, ícone de `enviado` vira o mesmo de moto da Home (Passo 33) e o de `em_execucao` vira uma fatia de pizza (era talheres) | Passos 25, 26, 33 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-35-icones-fases-kanban.md` |
| 36 | Os ícones da linha de cada fluxo na Home (cifrão, moto, fechar, excluir) ganham fundo redondo tipo botão (`IconButton.filledTonal`, mesmo padrão do Passo 13) | Passos 1, 29, 33 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-36-icones-redondos-home.md` |
| 37 | Diálogo de Excluir Fluxo de Atendimento passa a exigir 2 arrastos sequenciais ("Fase 1"/"Fase 2"), o segundo desabilitado até o primeiro ser concluído | Passo 11 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-37-exclusao-fluxo-duas-fases.md` |
| 38 | Ícone do botão "Enviar" no card `em_execucao` passa a usar a mesma moto (`Icons.two_wheeler`) da fase `enviado` no footer/cabeçalho do Kanban | Passos 24, 35 | **Concluído e validado** (2026-09-15) | `prompts-impl/passo-38-icone-enviar-card-moto.md` |
| 39 | Ao fechar um diálogo de confirmação ou formulário de entrada de dados sobre o Kanban (Tela 4), restaurar a rolagem na coluna de status que estava visível antes do refresh, em vez de voltar sempre para a primeira coluna | Passos 25, 28 | **Implementado, aguardando validação manual** | `prompts-impl/passo-39-restaurar-scroll-kanban.md` |
| 40 | Remover o header (`AppBar`) da Tela 4 (Execução do Fluxo de Atendimento) e criar um novo footer, abaixo do navegador de fases (Passo 25), com todos os elementos do header (identificador do fluxo, Novo Pedido, Filtrar Pedidos, Busca de Pedidos, Sair) | Passos 13, 25 | **Implementado, aguardando validação manual** | `prompts-impl/passo-40-footer-header-quadro-atendimento.md` |
| 41 | Na Tela 4 (Execução do Fluxo de Atendimento), pressionar o botão "Voltar" do Android deve solicitar a mesma confirmação já usada pelo botão "Sair" (Passo 9/40), em vez de sair direto sem perguntar | Passos 9, 40 | **Implementado, aguardando validação manual** | `prompts-impl/passo-41-confirmar-saida-botao-voltar.md` |
| 42 | Na Tela 4 (Execução do Fluxo de Atendimento), ao incluir um Pedido novo ou mudar o status de um Pedido existente, o Kanban deve rolar automaticamente até a coluna do status resultante, em vez de restaurar a coluna que estava visível antes (Passo 39) | Passos 25, 28, 39 | **Implementado, aguardando validação manual** | `prompts-impl/passo-42-navegar-coluna-apos-mudanca-status.md` |
| 43 | Em todo formulário de entrada/alteração de dados (Telas 3, 5, 6, 7, 8, 9 e Busca de Pedidos), posicionar o cursor no primeiro campo ao abrir a tela, abrindo o teclado virtual automaticamente | — | **Implementado, aguardando validação manual** | `prompts-impl/passo-43-autofocus-primeiro-campo-formularios.md` |
| 44 | Em todo formulário, o rótulo ("hint") dos campos passa a usar fonte um pouco menor e cinza claro, via tema global | — | **Em especificação** | `prompts-impl/passo-44-estilo-hints-formularios.md` |
| 45 | No Cadastro/Alteração de Pedido (Tela 5) e na Edição de Pedido em Execução (Tela 9), o campo Identificador do Pedido passa a ser editável a qualquer momento (permanecendo obrigatório nas duas), com fonte maior/negrito para o valor digitado, teclado numérico inicial e ícone de troca manual para texto completo | Passos 4, 6, 18 | **Implementado, aguardando validação manual** | `prompts-impl/passo-45-identificador-editavel-cadastro-pedido.md` |
| 46 | No footer de navegação da Tela 4, o selo de contagem de cada ícone de fase só é exibido quando a quantidade for maior que zero | Passos 25, 40 | **Implementado, aguardando validação manual** | `prompts-impl/passo-46-esconder-contagem-zero-navegador-fases.md` |

> Numeração de telas conforme `docs/controle-atendimento-prototype.md` §1 (Telas 1–9). Os Passos 9 a 31 são acréscimos/correções solicitados diretamente pelo usuário desta iniciativa de implementação — o Passo 9 não corresponde a nenhuma tela numerada do protótipo; o Passo 10 implementa "Finalizar Fluxo de Atendimento" (`docs/controle-atendimento-functional.md` §5.3), que nunca teve uma tela definida no protótipo original; o Passo 11 introduz melhorias de UI e uma nova chave técnica de ordenação na Home; o Passo 12 introduz carimbos de data/hora por mudança de estado do Pedido e sua exibição nos cards do Kanban; o Passo 13 introduz melhorias de UI e um filtro de visualização na Tela 4; o Passo 14 revisa a apresentação dos cards de Pedido e a ordem das colunas; o Passo 15 adiciona rótulo textual sob os ícones de ação dos cards; o Passo 16 é uma correção de bug de teclado nas Telas 7 e 8; o Passo 17 estende ao `pedido` a mesma chave técnica sequencial já aplicada a `fluxo_atendimento` no Passo 11; o Passo 18 revisa a regra de **P-4** (confirmado) do protótipo, substituindo o fluxo "Iniciar Pedido" por criação de Pedido novo com molde do mais recente; o Passo 19 é um ajuste de texto no rótulo de uma ação do card; o Passo 20 corrige um risco de colisão de arquivos de imagem introduzido pelo Passo 18; o Passo 21 fecha a lacuna de limpeza de imagens deixada como fora de escopo no Passo 11; o Passo 22 revê a obrigatoriedade do Tipo de Pagamento conforme o Tipo de Entrega; o Passo 23 adiciona validação de Endereço faltante na Tela 5 (a Tela 9 já validava); o Passo 24 adiciona diálogos de confirmação a 3 ações do Kanban e renomeia um rótulo; o Passo 25 adiciona um navegador de fases no footer da Tela 4; o Passo 26 dá destaque visual em cinza às 3 fases finais do delivery; o Passo 27 implementa a Busca de Pedidos (**P-7**), antes fora de escopo do protótipo; o Passo 28 sincroniza o destaque do navegador do footer com a coluna visível no Kanban; o Passo 29 adiciona uma consulta de valores de pagamento na Home; o Passo 30 adiciona máscara e validação ao campo Valor Pagamento; o Passo 31 reverte a obrigatoriedade da foto do Pedido (**DS-7**, antes confirmado); o Passo 32 limita o tamanho do campo Valor Pagamento a 8 caracteres; o Passo 33 adiciona a Prestação de Contas com o Motoqueiro na Home; o Passo 34 remove Pedidos cancelados do popup de Valores de Pagamento; o Passo 35 troca os ícones das fases `enviado` e `em_execucao` no Kanban; o Passo 36 dá fundo redondo aos ícones da linha de fluxo na Home; o Passo 37 torna a exclusão de fluxo um arrasto em 2 fases sequenciais; o Passo 38 troca o ícone do botão "Enviar" do card pela mesma moto da fase `enviado`; o Passo 44 aplica um estilo global (fonte menor, cinza claro) ao rótulo dos campos de todos os formulários; o Passo 45 torna o campo Identificador do Pedido (Tela 5) editável a qualquer momento, com destaque visual e teclado numérico inicial; o Passo 46 esconde o selo de contagem zero no footer de navegação da Tela 4 — nenhum desses trinta e dois tem correspondência prévia em `docs/`, exceto o Passo 27, que implementa uma funcionalidade já especificada em `functional.md` §5.5, e o Passo 31, que reverte uma decisão documentada em `data-structure.md` (ver os respectivos prompts de passo).

## 5. Fluxo de Trabalho por Passo

1. Este documento é atualizado com o status **"Em especificação"** para o passo.
2. É criado `prompts-impl/passo-NN-<slug>.md` com o prompt de implementação detalhado do passo.
3. O prompt é executado — implementação em `code/frontend/controle-atendimento-app`.
4. O usuário valida manualmente o resultado (dispositivo/emulador Android).
5. Status atualizado para **"Concluído e validado"** neste documento, com a data da validação.
6. O próximo passo é especificado (volta ao item 1).

## 6. Status Atual

- **Passo 1:** concluído e validado em 2026-09-14 — implementado em `code/frontend/controle-atendimento-app`, testado em dispositivo Android real.
- **Passo 2:** concluído e validado em 2026-09-14.
- **Passo 3:** concluído e validado em 2026-09-14.
- **Passo 4:** concluído e validado em 2026-09-14.
- **Passo 5:** concluído e validado em 2026-09-14.
- **Passo 6:** concluído e validado em 2026-09-14.
- **Passo 7:** concluído e validado em 2026-09-14.
- **Passo 8:** em especificação — prompt criado em `prompts-impl/passo-08-devolucao-entrega.md`, aguardando implementação e validação. Último passo do plano original (telas do protótipo).
- **Passo 9:** em especificação — prompt criado em `prompts-impl/passo-09-sair-quadro-atendimento.md`, aguardando implementação e validação. Acréscimo solicitado pelo usuário em 2026-09-14, fora da numeração original de telas.
- **Passo 10:** em especificação — prompt criado em `prompts-impl/passo-10-fechar-fluxo-atendimento.md`, aguardando implementação e validação. Acréscimo solicitado pelo usuário em 2026-09-14: implementa "Finalizar Fluxo de Atendimento" (`functional.md` §5.3) a partir da Home, algo que não tinha tela nem UI definida até então.
- **Passo 11:** concluído e validado em 2026-09-14 — nova chave sequencial `id` em `fluxo_atendimento` para ordenação real por criação (substitui a ordenação por `identificador`, que é texto livre editável), layout em cards com novo background, ícone de fechamento trocado para `done_all`, cor de fundo diferenciada para fluxos fechados, e exclusão total (hard delete em cascata) de um Fluxo de Atendimento e seus Pedidos.
- **Passo 12:** concluído e validado em 2026-09-14 — 8 novos campos de data/hora em `pedido` (um por estado da máquina de estados + um para o cancelamento), preenchidos na transição de entrada de cada estado, exibidos no título do card do Kanban (`identificador (HH:mm)`) e, no caso de cancelamento, com destaque visual e o motivo abaixo.
- **Passo 13:** concluído e validado em 2026-09-14 — remoção do ícone de voltar do header da Tela 4, fundo sólido por coluna do Kanban em tom único (`deepOrange`, alinhado ao `colorSchemeSeed` do app), escurecendo da esquerda para a direita, com espaçamento e cantos arredondados entre colunas e título centralizado; botões do header em variante mais estilizada (`IconButton.filledTonal`); ordenação decrescente por `dataHoraStatusAtual` em cada coluna; novo botão de filtro de visualização (Todos/Somente Cancelados/Somente Não Cancelados) com indicação visual de filtro ativo; paleta dos cards revista (branco / vermelho mais acentuado para cancelados) para não repetir a cor das colunas.
- **Passo 14:** concluído e validado em 2026-09-14 — `_campos()` de `PedidoCard` unificado (sem distinção por status, cada campo só aparece se tiver valor) para que `aguardando_atendimento` também mostre cadastro parcial; botões de ação do rodapé viram `IconButton` com tooltip, centralizados horizontalmente; nome do cliente sem label, em negrito; demais campos com ícone no lugar do label; imagem do pedido clicável, abrindo em tela cheia (`ImagemPedidoTelaCheia`) com botão de fechar; Endereço reposicionado logo abaixo de Entrega, Mesa logo abaixo de Cliente; Observação em negrito; `PedidoStatus.retiradoNoBalcao` movido para o final da declaração do enum, o que já move a coluna correspondente para o final do Kanban.
- **Passo 15:** concluído e validado em 2026-09-14 — cada ícone de ação do rodapé dos cards de Pedido passa a exibir o nome da ação em cinza logo abaixo do ícone (antes só disponível via `tooltip`), sem mudar ícones nem comportamento.
- **Passo 16:** concluído e validado em 2026-09-14 — corrigido o campo "Motivo" da Tela 7 (Cancelamento) e o campo "Motivo da Devolução" da Tela 8 (mesma causa raiz: `TextFormField` multilinha sem `textInputAction`), adicionando `textInputAction: TextInputAction.done` + `onFieldSubmitted` para fechar o teclado.
- **Passo 17:** concluído e validado em 2026-09-14 — nova chave técnica `id INTEGER PRIMARY KEY AUTOINCREMENT` em `pedido` (mesma decisão do Passo 11 para `fluxo_atendimento`), `identificador` passou a `TEXT NOT NULL UNIQUE`; `PedidoRepository.salvar()` reescrito para não depender mais de `ConflictAlgorithm.replace` (checagem de existência seguida de `insert` ou `update` explícito), preservando o `id` da linha a cada edição.
- **Passo 18:** concluído e validado em 2026-09-14 — ao confirmar o identificador na Tela 5 (fluxo "Novo Pedido"), busca em todos os fluxos (correspondência sempre exata) o Pedido mais recente com aquele identificador, pré-preenche Cliente/Entrega/Pagamento/Endereço (não Observação) e sempre grava como Pedido novo — substitui o fluxo "Iniciar Pedido" de **P-4**. `pedido.identificador` perdeu toda constraint de unicidade (decisão explícita do usuário); `PedidoRepository` (`excluir`, `atualizarStatus`, `salvar`) e as 5 telas que gravam Pedido passaram a operar por `id` em vez de `identificador`.
- **Passo 19:** concluído e validado em 2026-09-15 — rótulo do ícone de ação "Atendimento" (card `aguardando_atendimento`, Tela 4) renomeado para "Atender"; mesmo ícone e ação, só o texto muda.
- **Passo 20:** concluído e validado em 2026-09-15 — `ImagemPedidoStorage` passou a nomear o arquivo pelo `id` do Pedido em vez do `identificador` (que deixou de ser único no Passo 18, podendo causar colisão/sobrescrita de imagens entre Pedidos diferentes).
- **Passo 21:** concluído e validado em 2026-09-15 — `FluxoAtendimentoRepository.excluirComPedidos` passou a apagar também os arquivos de imagem dos Pedidos do fluxo excluído (novo método `ImagemPedidoStorage.excluir`), fechando a lacuna deixada como fora de escopo no Passo 11.
- **Passo 22:** concluído e validado em 2026-09-15 — Tipo de Pagamento só é exigido para o Pedido alcançar `em_atendimento` quando o Tipo de Entrega é `delivery` (Tela 5, `cadastroCompleto`) e o `validator` do dropdown na Tela 9 passou a seguir a mesma regra; para `retirada_balcao` pode ficar em branco.
- **Passo 23:** concluído e validado em 2026-09-15 — na Tela 5, ao editar um Pedido já além de `aguardando_atendimento`, Endereço passou a bloquear o Gravar (via `validator` + `_dadosFormKey.currentState!.validate()`, que antes nunca era chamado nessa tela) se o Tipo de Entrega for `delivery`. Levantamento confirmou que a Tela 9 já fazia exatamente isso, sem precisar de mudança.
- **Passo 24:** concluído e validado em 2026-09-15 — rótulo "Enviado" do card `em_execucao` renomeado para "Enviar"; novo diálogo de confirmação simples (mesmo padrão do "Excluir Pedido") antes de "Enviar", "Retirado" (card `em_execucao`) e "Entregue" (card `enviado`) — "Editar", "Cancelar" e "Devolvido" continuam sem confirmação.
- **Passo 25:** concluído e validado em 2026-09-15 — novo footer na Tela 4 com 7 ícones (um por fase do Pedido, mesma ordem das colunas do Kanban desde o Passo 14), cada um com fundo redondo na cor real da coluna correspondente (`_corColuna`, Passo 13) e a contagem de cards abaixo; tocar rola o `SingleChildScrollView` do Kanban (`_QuadroView` virou `StatefulWidget` para ter um `ScrollController`) até a coluna daquela fase ficar alinhada à esquerda. O mesmo ícone também passou a aparecer no cabeçalho de cada coluna.
- **Passo 26:** concluído e validado em 2026-09-15 — colunas e ícones do footer de `entregue`, `devolvido` e `retirado_no_balcao` passaram a usar cinza sólido (progressivamente mais escuro entre si), em vez da cor laranja sólida (`_corColuna`) usada pelas demais 4 fases (`enviado` incluso). Passou por duas revisões em conversa: conjunto de fases corrigido (era `enviado, entregue, devolvido`) e efeito trocado de degradê para cor sólida.
- **Passo 27:** concluído e validado em 2026-09-15 — implementa **P-7** (`functional.md` §5.5), até então fora de escopo da UI conforme o protótipo original. Usuário estendeu os campos de busca de `functional.md` §5.5 (nome_cliente, identificador, mesa, endereco) com mais dois: observação e restrições — total de 6 campos, um por vez (mútua exclusão), Mesa com busca exata e os demais parciais (`LIKE`). Resultados em tela separada (`ResultadoBuscaPedidosScreen`), agrupados por status na mesma ordem/critério de ordenação do Kanban (Passos 13/14), exibidos em blocos verticais reaproveitando cores/ícones das colunas (extraídos para `fase_pedido_visual.dart`, Passos 13/25/26), com os mesmos botões de ação do Kanban (somente leitura quando o fluxo está fechado) e botão "Limpar" no formulário.
- **Passo 28:** concluído e validado em 2026-09-15 — o ícone do navegador de fases do footer (Passo 25) correspondente à coluna visível no Kanban ganha um anel de destaque, sincronizado tanto com a rolagem manual do Kanban (via listener no `ScrollController`) quanto com o toque nos próprios ícones do footer (`_rolarParaFase`); o destaque só é exibido em modo retrato (`MediaQuery.orientationOf`).
- **Passo 29:** concluído e validado em 2026-09-15 — novo ícone de cifrão em cada linha de fluxo da Home (antes do ícone de fechamento), abrindo um popup (`ValoresPagamentoDialog`) com os Pedidos daquele fluxo que têm `valor_pagamento` preenchido e o total somado; nova consulta `PedidoRepository.listComValorPagamento`. Cada item mostra "(Tipo de Pagamento) Nome" em negrito e o Valor abaixo (revisão: Identificador deixou de aparecer como linha própria).
- **Passo 30:** concluído e validado em 2026-09-15 — campo Valor Pagamento (Telas 6 e 9) ganhou uma máscara de entrada (`ValorPagamentoInputFormatter`, só dígitos e um separador decimal, no máximo 2 casas) e um `validator` (`validarValorPagamento`) ao gravar, garantindo um valor monetário válido; campo continua opcional. Ambos extraídos para `lib/core/widgets/valor_pagamento_input_formatter.dart`, compartilhado entre as duas telas.
- **Passo 31:** concluído e validado (2026-09-15) — `prompts-impl/passo-31-foto-opcional.md`. Acréscimo solicitado pelo usuário em 2026-09-15: a foto do Pedido deixa de ser obrigatória nas Telas 6 e 9, revertendo **DS-7** *(confirmado)*/`data-structure.md` §3. Levantamento mostrou que a Tela 9 já não bloqueava por falta de foto; só a Tela 6 precisou de mudança (`_onGravar` deixou de checar `_foto == null`, campo `_erroFoto` removido). `flutter analyze` sem problemas; build instalado e verificado via logcat em ambos os dispositivos conectados.
- **Passo 32:** concluído e validado (2026-09-15) — `prompts-impl/passo-32-limite-valor-pagamento.md`. Acréscimo solicitado pelo usuário em 2026-09-15: o campo Valor Pagamento (Telas 6 e 9) ganha um limite de 8 caracteres, incluindo o separador decimal. Implementado como uma checagem a mais (`if (texto.length > 8) return oldValue;`) no `ValorPagamentoInputFormatter` já compartilhado (Passo 30) — nenhuma mudança nas telas. `flutter analyze` sem problemas; build instalado e verificado via logcat em ambos os dispositivos conectados.
- **Passo 33:** concluído e validado (2026-09-15) — `prompts-impl/passo-33-prestacao-contas-motoqueiro.md`. Acréscimo solicitado pelo usuário em 2026-09-15: novo ícone (`Icons.two_wheeler`, escolhido via `AskUserQuestion`) em cada linha de fluxo da Home, abrindo um popup no mesmo formato do Passo 29 (`ValoresPagamentoDialog`) que lista os Pedidos `delivery` do fluxo (nome + horário de envio entre parênteses, endereço abaixo) e totaliza o número de entregas. Nova consulta `PedidoRepository.listDelivery`; novo `PrestacaoContasMotoqueiroDialog`. Critério de seleção corrigido em seguida pelo usuário: além de `delivery`, exige status `enviado`, `entregue` ou `devolvido` (Pedidos ainda não enviados não entram).
- **Passo 34:** concluído e validado (2026-09-15) — `prompts-impl/passo-34-valores-pagamento-exclui-cancelados.md`. Acréscimo solicitado pelo usuário em 2026-09-15: `PedidoRepository.listComValorPagamento` (Passo 29) ganhou `AND cancelado = 0` no filtro, para que o popup de Valores de Pagamento não mostre Pedidos cancelados mesmo que tenham valor de pagamento lançado; nenhuma mudança em `ValoresPagamentoDialog` (o total já derivava da lista retornada). `flutter analyze` sem problemas; build instalado e verificado via logcat em ambos os dispositivos conectados.
- **Passo 35:** concluído e validado (2026-09-15) — `prompts-impl/passo-35-icones-fases-kanban.md`. Acréscimo solicitado pelo usuário em 2026-09-15: no mapa compartilhado `iconePorFase` (`fase_pedido_visual.dart`), a fase `enviado` passou a usar `Icons.two_wheeler` (mesmo ícone da Prestação de Contas com o Motoqueiro, Passo 33) e a fase `em_execucao` passou de talheres (`Icons.restaurant_outlined`) para uma fatia de pizza (`Icons.local_pizza_outlined`, escolhido via `AskUserQuestion` depois que o usuário rejeitou as opções de forno/churrasqueira oferecidas e pediu um ícone de pizza). Mudança concentrada nesse único mapa compartilhado, propagando para o footer/cabeçalho do Kanban e os blocos da Busca de Pedidos sem tocar nas 3 telas. `flutter analyze` sem problemas; build instalado e verificado via logcat em ambos os dispositivos conectados.
- **Passo 36:** concluído e validado (2026-09-15) — `prompts-impl/passo-36-icones-redondos-home.md`. Acréscimo solicitado pelo usuário em 2026-09-15: os 4 ícones da linha de cada fluxo na Home (cifrão, moto, fechar, excluir) ganharam um fundo redondo tipo botão, reaproveitando o mesmo padrão do Passo 13 (`IconButton` → `IconButton.filledTonal`), sem mudança de comportamento. `flutter analyze` sem problemas; build instalado e verificado via logcat em ambos os dispositivos conectados.
- **Passo 37:** concluído e validado (2026-09-15) — `prompts-impl/passo-37-exclusao-fluxo-duas-fases.md`. Acréscimo solicitado pelo usuário em 2026-09-15: `ExcluirFluxoDialog` (agora `StatefulWidget`) passou a exigir 2 `SlideToConfirmButton`s sequenciais ("Arraste para excluir - Fase 1"/"Fase 2"), a Fase 2 desabilitada (visual e funcionalmente) até a Fase 1 ser concluída. `SlideToConfirmButton` (Passo 11) ganhou o parâmetro `enabled`. `FecharFluxoDialog` não foi afetado — pedido do usuário foi só sobre a exclusão. `flutter analyze` sem problemas; build instalado e verificado via logcat em ambos os dispositivos conectados.
- **Passo 38:** concluído e validado (2026-09-15) — `prompts-impl/passo-38-icone-enviar-card-moto.md`. Acréscimo solicitado pelo usuário em 2026-09-15: o botão "Enviar" do card `em_execucao` (`PedidoCard._buildFooter`, Passo 24) trocou `Icons.local_shipping_outlined` por `Icons.two_wheeler`, o mesmo ícone já usado para a fase `enviado` no footer/cabeçalho do Kanban (Passo 35). `flutter analyze` sem problemas; build instalado e verificado via logcat no dispositivo `R9XYB04Y8QV` (`4A414Y75Q` não estava conectado nesta rodada). `flutter analyze` sem problemas; build instalado e verificado via logcat em ambos os dispositivos conectados.
- **Passo 39:** implementado em 2026-09-15, aguardando validação manual — `prompts-impl/passo-39-restaurar-scroll-kanban.md`. Acréscimo solicitado pelo usuário em 2026-09-15: fechar um diálogo de confirmação ou formulário de entrada de dados sobre o Kanban (Tela 4) recarregava os dados (`QuadroAtendimentoController.load()`) e sempre voltava a rolagem para a primeira coluna. Causa raiz: `build` troca `_buildQuadro` por `LoadingView` enquanto `loading == true`, desmontando o `SingleChildScrollView`; ao remontar, a nova `ScrollPosition` nasce em offset 0. Corrigido com um listener no `QuadroAtendimentoController` (`_aoAtualizarController`, adicionado em `initState`) que detecta a transição `loading: true -> false` e restaura a rolagem (`jumpTo`, sem animação) na coluna guardada em `_indiceFaseVisivel` (já mantido pelo Passo 28) — cobre de uma vez só as 5 telas de Pedido e os 4 diálogos de confirmação do card (Passo 24) que chamam `load()`, sem duplicar lógica em cada ponto de chamada. `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV` (`4A414Y75Q` não estava conectado nesta rodada).
- **Passo 40:** implementado em 2026-09-16, aguardando validação manual — `prompts-impl/passo-40-footer-header-quadro-atendimento.md`. Acréscimo solicitado pelo usuário em 2026-09-16: `Scaffold.appBar` removido da Tela 4; `body` envolvido em `SafeArea(bottom: false, ...)` para preservar o respiro da status bar; `bottomNavigationBar` passou a ser uma `Column` com `_buildNavegadorFases` (Passo 25, agora `SafeArea(bottom: false, ...)`) seguido do novo `_buildHeaderFooter` (`SafeArea(top: false, ...)`) reunindo o identificador do fluxo (título) e os 4 ícones de ação (Novo Pedido, Filtrar Pedidos, Busca de Pedidos, Sair) — mesmas condições/comportamento de antes, só reposicionamento. **Revisão visual no mesmo dia** (sem passo novo, direto no código), em 2 rodadas: primeira rodada aplicou fundo `colorScheme.primary` (deepOrange, escolhido via `AskUserQuestion`) às duas linhas do footer juntas (navegador de fases + linha de ações) e alterou o destaque/contagem do navegador de fases para branco; o usuário então pediu para restringir o estilo só à linha de baixo — navegador de fases (Passo 25/28) revertido ao visual original (destaque laranja, contagem cinza, sem sombra no ícone). Estado final da 2ª rodada: só `_buildHeaderFooter` (identificador do fluxo + ações) tem fundo `colorScheme.primary` com sombra própria separando-o do navegador de fases acima; botões de ação são `IconButton.filled` com cores invertidas (fundo claro, ícone na cor da marca — estilo "preenchido sólido", também escolhido via `AskUserQuestion`). **3ª rodada** (mesmo dia): usuário pediu para deixar os ícones do navegador de fases "mais fancy" — via `AskUserQuestion`, escolheu "Sombra + elevação no ativo" e "Badge sobreposto" para a contagem. `_iconeFase` reescrito: `AnimatedContainer` com sombra (`boxShadow`, mais forte/maior quando ativo) e `AnimatedScale` (ícone cresce ~15% quando ativo); a contagem deixou de ser texto abaixo do ícone e virou um selo circular (`Positioned` no canto superior direito, fundo `colorScheme.primary`, borda branca) sobreposto ao círculo do ícone via `Stack`. `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV` (`4A414Y75Q` não estava conectado em nenhuma das rodadas). **Correção de bug reportada logo em seguida:** ao ficar ativo, o ícone "pulava" para a direita/baixo, descentralizado do próprio fundo circular. Causa: `AnimatedContainer` variava `padding` (10→13) e a borda (ausente→presente, largura 3) conforme `ativo`, mudando o tamanho do círculo; como esse círculo é o único filho não-posicionado do `Stack` (que se ajusta exatamente ao tamanho dele) e o `Stack` alinha filhos não-posicionados a partir do canto superior esquerdo, o crescimento do círculo "empurrava" o conteúdo para baixo/direita em vez de manter o centro. Corrigido tornando o tamanho do círculo **constante** em qualquer estado — borda sempre reservada (largura 3 fixa, cor `Colors.transparent` quando inativo) e padding fixo em 10 — e movendo o efeito de "crescer quando ativo" para um `AnimatedScale` (transformação de pintura, não afeta layout/posição) envolvendo todo o círculo. `flutter analyze` sem problemas; build reinstalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`. **4ª rodada de estilo (footer de ação):** usuário pediu ícones quadrados com borda arredondada e um fundo mais "fancy" com cantos arredondados no footer — via `AskUserQuestion`, escolheu "Gradiente diagonal" para o fundo e raio "Médio ~16px". `_buildHeaderFooter`: fundo passou de `color` sólido para `gradient: LinearGradient` (3 tons de `colorScheme.primary`, de mais escuro a mais claro, topLeft→bottomRight) com `borderRadius` só nos cantos superiores (`_raioCantosFooter = 16`, únicos visíveis já que o footer encosta na borda inferior da tela); os 4 `IconButton.filled` ganharam `shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16))` no `estiloBotao` compartilhado, virando quadrados de canto arredondado em vez de círculos. Navegador de fases (linha de cima) não foi tocado. `flutter analyze` sem problemas; build reinstalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`. **5ª rodada (correção):** usuário pediu para tirar o arredondamento dos cantos do fundo do footer de ação — `borderRadius` removido do `BoxDecoration` (constante `_raioCantosFooter` mantida, ainda usada só pelo `shape` dos botões); gradiente e sombra continuam. `flutter analyze` sem problemas; build reinstalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`. **6ª rodada:** usuário pediu sombra, cor melhor e ícones mais "fancy" nos 4 botões do footer de ação — via `AskUserQuestion`, escolheu "Branco com gradiente sutil" para a cor e a variante "rounded" dos ícones (`_rounded`). Novo helper `_botaoFooterAcao`: cada botão passou a ser um `Container` decorado (gradiente diagonal branco→`Color(0xFFFFE0B2)`, sombra `black45`/blurRadius 6/offset (0,3)) envolvendo um `Material(color: transparent)` + `IconButton` (transparente, só o ícone laranja), em vez do `IconButton.filled` liso de antes — evita sombra "cortada" pelos cantos arredondados porque o `Material` interno usa o mesmo `borderRadius` do `Container` externo para conter o efeito de toque. Ícones trocados para as variantes `_rounded` (`Icons.add_rounded`, `Icons.filter_list_rounded`/`Icons.filter_alt_rounded`, `Icons.search_rounded`, `Icons.logout_rounded`). `flutter analyze` sem problemas; build reinstalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`. **7ª rodada:** usuário reportou que os ícones do navegador de fases (footer de navegação) continuavam descentralizados no fundo do próprio botão, mesmo após a correção da rodada anterior, e pediu também um fundo leve para esse footer — via `AskUserQuestion`, escolheu "Laranja bem claro" (`Colors.deepOrange.shade50`). Correção definitiva do centro: o círculo de cada ícone (`AnimatedContainer` em `_iconeFase`) passou a ter `width`/`height` fixos (nova constante `_diametroIconeFase = 50.0`) e `alignment: Alignment.center`, em vez de depender de `padding` simétrico para centralizar o `Icon` — mais robusto, elimina qualquer chance de o ícone descolar do centro independente do estado da borda de destaque. `_buildNavegadorFases` ganhou um `ColoredBox(color: Colors.deepOrange.shade50)` envolvendo o `SafeArea`. `flutter analyze` sem problemas; build reinstalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`. **8ª rodada (reversão):** usuário pediu para tirar a cor de fundo do footer de navegação, voltando ao estado anterior — `ColoredBox` removido, `SafeArea` volta a ser o widget raiz de `_buildNavegadorFases` (sem fundo/transparente). Correção de centralização dos ícones (`width`/`height` fixos + `alignment: Alignment.center`) mantida. `flutter analyze` sem problemas; build reinstalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`.
- **Passo 41:** implementado em 2026-09-16, aguardando validação manual — `prompts-impl/passo-41-confirmar-saida-botao-voltar.md`. Acréscimo solicitado pelo usuário em 2026-09-16: na Tela 4, o botão "Voltar" do Android deve pedir a mesma confirmação já usada pelo botão "Sair" (Passo 9/40), em vez de sair direto sem perguntar. `_QuadroViewState.build` envolvido em `PopScope(canPop: false, onPopInvokedWithResult: ...)` chamando `_confirmarSaida(context)` (sem nenhuma mudança nela) quando `!didPop` — reaproveita o mesmo diálogo/destino do botão "Sair" do footer de ação, sem duplicar lógica. `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`.
- **Passo 42:** implementado em 2026-09-16, aguardando validação manual — `prompts-impl/passo-42-navegar-coluna-apos-mudanca-status.md`. Acréscimo solicitado pelo usuário em 2026-09-16: ao incluir um Pedido novo ou mudar o status de um Pedido existente na Tela 4, o Kanban deve rolar automaticamente até a coluna do status resultante, em vez de restaurar a coluna anterior (Passo 39). `CadastroPedidoScreen`/`ExecucaoPedidoScreen`/`DevolucaoEntregaScreen` passaram a fechar com `Navigator.pop(novoStatus)` no caminho de sucesso (`novoStatus` já calculado em Cadastro; constante `PedidoStatus.emExecucao`/`.devolvido` nas outras duas) — "Fechar"/"Cancelar" continuam sem valor. Em `_QuadroViewState`: novo campo `_statusAlvoAposRecarga`; `_abrirCadastroPedido`/`_abrirExecucaoPedido`/`_abrirDevolucao` capturam o retorno tipado (`push<PedidoStatus>`) e o atribuem quando não-nulo; novo `_atualizarStatusENavegar` grava o alvo antes de delegar para `controller.atualizarStatus`, usado pelas ações rápidas do card via novo callback `onAtualizarStatus` em `_KanbanColuna` (substituindo as 3 chamadas diretas a `controller.atualizarStatus`); `_restaurarPosicaoScroll` (Passo 39) passou a dar prioridade ao status-alvo quando presente, rolando de forma **animada** (como `_rolarParaFase`) em vez de `jumpTo`, caindo de volta na restauração silenciosa de `_indiceFaseVisivel` quando não há status-alvo (Tela 9, Cancelamento, exclusão). `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`.
- **Passo 43:** implementado em 2026-09-16, aguardando validação manual — `prompts-impl/passo-43-autofocus-primeiro-campo-formularios.md`. Acréscimo solicitado pelo usuário em 2026-09-16: em todo formulário de entrada/alteração de dados, posicionar o cursor no primeiro campo (editável) ao abrir a tela, abrindo o teclado automaticamente. `autofocus: true` adicionado ao primeiro campo editável das 7 telas identificadas (Tela 3 Identificador; Tela 6 Mesa; Tela 7 Motivo; Tela 8 Motivo da Devolução; Tela 9 Nome do Cliente, pulando o Identificador somente leitura; Busca de Pedidos Identificador, via novo parâmetro `autofocus` no helper `_campoBusca`). Tela 5 (2 estágios) recebeu `autofocus: !_identificadorConfirmado` no campo Identificador e `autofocus: true` no campo Nome do Cliente — como o `autofocus` do Flutter dispara na primeira inserção de cada widget na árvore (não a cada rebuild), isso cobre tanto a abertura via "Novo Pedido" (foco no Identificador, depois passa para Nome do Cliente ao confirmar) quanto via "Atendimento"/"Editar" (Identificador já `readOnly`, foco direto em Nome do Cliente), sem precisar de `FocusNode` manual. `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`. **Follow-up no mesmo dia** (sem passo novo, mesma tela): a ação "Concluído" do teclado no campo Identificador (ainda não confirmado) passou a acionar `_onConfirmarIdentificador()` diretamente (`textInputAction: TextInputAction.done` + `onFieldSubmitted`, guardado por `!_identificadorConfirmado && !_buscando`), evitando que o usuário precise fechar o teclado manualmente antes de tocar em "Confirmar". `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`.
- **Passo 44:** implementado em 2026-09-17, aguardando validação manual — `prompts-impl/passo-44-estilo-hints-formularios.md`. Acréscimo solicitado pelo usuário em 2026-09-17: em todo formulário do app, o rótulo dos campos passa a usar fonte um pouco menor e cor cinza claro, aplicado via `InputDecorationTheme` no tema global (`app_theme.dart`), sem precisar editar campo por campo. **Revisão no mesmo dia:** o usuário pediu que, ao selecionar/preencher o campo (rótulo "flutuante"), o texto volte ao tamanho/cor originais — só a aparência "em repouso" deve ficar menor/cinza. `labelStyle` trocado de `TextStyle` fixo para `WidgetStateTextStyle.resolveWith(...)` (sem definir `floatingLabelStyle`): por comportamento documentado da própria classe do Flutter, um `WidgetStateTextStyle` usado fora de uma resolução de estado equivale a um `TextStyle()` vazio, então deixa de "vazar" para o cálculo do rótulo flutuante (que cai de volta em `labelStyle` só quando `floatingLabelStyle` não está definido), enquanto o rótulo em repouso continua resolvendo o estilo customizado normalmente. `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`. **Revisão 2, mesmo dia:** `fontSize` do rótulo em repouso aumentado 20% (`13` → `15.6`), pedido do usuário. `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`.
- **Passo 45:** implementado em 2026-09-17, aguardando validação manual — `prompts-impl/passo-45-identificador-editavel-cadastro-pedido.md`. Acréscimo solicitado pelo usuário em 2026-09-17: no Cadastro/Alteração de Pedido (Tela 5), o campo Identificador do Pedido deixa de ficar somente leitura após confirmado (`readOnly: _identificadorConfirmado` removido), passa a exibir o valor digitado em fonte maior e negrito, e o teclado abre inicialmente no modo numérico (sem bloquear a troca para texto). Tela 9 (identificador somente leitura) fica fora de escopo. **Correção no mesmo dia** (mesmo passo, sem número novo): o campo ter ficado sempre editável abriu uma lacuna de validação — "Gravar" só validava `_dadosFormKey`, nunca o `Form` do Identificador; `_onGravar` passou a validar as duas `Form`s, tornando o Identificador efetivamente obrigatório também depois de confirmado (não só antes). `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`. **Revisão 2, mesmo dia — estende à Tela 9:** o campo Identificador de `edicao_pedido_execucao_screen.dart` também deixou de ser somente leitura (`readOnly: true` removido, novo `_identificadorController`), ganhou a mesma fonte maior/negrito da Tela 5 e passou a ser obrigatório (`validator: _obrigatorio`, reaproveitando o helper já existente na tela); `_onGravar` já validava a única `Form` da tela, então a obrigatoriedade passou a valer sem mudança extra de validação. `identificador` gravado passou a vir de `_identificadorController.text.trim()` em vez de `widget.pedido.identificador`. Diferente da Tela 5, esta tela não ganhou teclado numérico inicial (não solicitado). `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`. **Revisão 3, mesmo dia — troca manual de teclado (Tela 5):** o usuário reportou que, no teclado do dispositivo de teste (Samsung Keyboard), o modo numérico não oferece nenhuma alternância nativa para texto completo. Solução escolhida via `AskUserQuestion` ("ícone que muda", 123 ↔ ABC): novo `_identificadorTeclaNumerica` (estado) e `_identificadorFocusNode`; `keyboardType` do campo passou a depender desse estado; `decoration.suffixIcon` ganhou um `IconButton` (`Icons.numbers`/`Icons.abc`) que alterna o estado e força o teclado a recarregar (`unfocus()` seguido de `requestFocus()` num `addPostFrameCallback`). Tela 9 não é afetada (nunca teve teclado numérico nesse campo). `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`. **Correção no mesmo dia:** o ícone mostrava o modo atual do teclado; o usuário pediu para inverter — passou a mostrar o modo para o qual vai mudar ao tocar (`Icons.abc` quando o teclado está em números, `Icons.numbers` quando está em texto), alinhado ao sentido que o `tooltip` já tinha. `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`. **Revisão 4, mesmo dia — estende à Tela 9:** o mesmo comportamento da Revisão 3 (teclado numérico inicial + ícone de troca manual 123/ABC, já no sentido "modo de destino") replicado em `edicao_pedido_execucao_screen.dart` — mesmo `_identificadorTeclaNumerica`/`_identificadorFocusNode`/`_alternarTecladoIdentificador`. `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`.
- **Passo 46:** implementado em 2026-09-17, aguardando validação manual — `prompts-impl/passo-46-esconder-contagem-zero-navegador-fases.md`. Acréscimo solicitado pelo usuário em 2026-09-17: no footer de navegação da Tela 4 (`_iconeFase`, Passo 25/40), o `Positioned` do selo de contagem sobreposto ao ícone de cada fase passou a ser condicional (`if (quantidade > 0) ...` na lista de filhos do `Stack`) — quando a fase não tem nenhum Pedido, o ícone aparece sem selo. `flutter analyze` sem problemas; build instalado e app verificado sem crash via logcat no dispositivo `R9XYB04Y8QV`.
